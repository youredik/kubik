# 🚀 MCP Servers Integration Guide

## Обзор

В рамках проекта **baget-shop-next** мы реализовали интеграцию **MCP (Model Context Protocol) серверов**, которые значительно расширяют возможности Kilo Code для веб-разработки.

## 🎯 Что такое MCP?

**Model Context Protocol (MCP)** - это открытый протокол для безопасного подключения внешних инструментов к AI-ассистентам. MCP серверы предоставляют структурированный доступ к различным ресурсам и инструментам.

## 📦 Реализованные MCP серверы

### 1. **Filesystem Server** 🗂️
**Файл:** `mcp-servers/filesystem-server.js`

**Возможности:**
- ✅ Чтение содержимого файлов
- ✅ Просмотр структуры директорий
- ✅ Запись файлов
- ✅ Создание директорий
- ✅ Удаление файлов

**Безопасность:** Ограничен указанными директориями

### 2. **Terminal Server** 💻
**Файл:** `mcp-servers/terminal-server.js`

**Возможности:**
- ✅ Выполнение безопасных команд терминала
- ✅ Поддержка команд: `ls`, `cd`, `pwd`, `git`, `npm`, `node`, `yarn`, `pnpm`

**Безопасность:** Только whitelist команд

### 3. **Web Reader Server** 🌐
**Файл:** `mcp-servers/web-reader-server.js`

**Возможности:**
- ✅ Извлечение контента с веб-страниц
- ✅ Поиск в интернете через DuckDuckGo
- ✅ Извлечение текста, ссылок, изображений

**Безопасность:** Только разрешенные домены

### 4. **Browser Automation Server** 🤖
**Файл:** `mcp-servers/browser-server.js`

**Возможности:**
- ✅ Навигация по веб-страницам
- ✅ Создание скриншотов
- ✅ Клик по элементам
- ✅ Ввод текста в формы
- ✅ Извлечение текста и HTML
- ✅ Ожидание элементов

**Технология:** Playwright в headless режиме

## ⚙️ Конфигурация

### Основной конфигурационный файл: `.kilorc.json`

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "node",
      "args": ["/Users/ed/dev/kubik/mcp-servers/filesystem-server.js", "/Users/ed/dev/kubik"],
      "env": { "NODE_ENV": "development" }
    },
    "terminal": {
      "command": "node",
      "args": ["/Users/ed/dev/kubik/mcp-servers/terminal-server.js"],
      "env": {
        "ALLOWED_COMMANDS": "ls,cd,pwd,git,npm,node,yarn,pnpm,echo,cat,head,tail,grep,find"
      }
    },
    "web-reader": {
      "command": "node",
      "args": ["/Users/ed/dev/kubik/mcp-servers/web-reader-server.js"],
      "env": {
        "ALLOWED_DOMAINS": "github.com,stackoverflow.com,developer.mozilla.org,react.dev,nextjs.org"
      }
    },
    "browser-automation": {
      "command": "node",
      "args": ["/Users/ed/dev/kubik/mcp-servers/browser-server.js"],
      "env": {
        "PLAYWRIGHT_BROWSERS_PATH": "/tmp/playwright-browsers"
      }
    }
  },
  "mcpEnabled": true,
  "project": {
    "name": "baget-shop-next",
    "type": "nextjs",
    "nodeVersion": "24.12.0",
    "framework": "Next.js 16",
    "database": "SQLite + Prisma",
    "testing": "Jest + React Testing Library"
  }
}
```

## 🚀 Установка и запуск

### 1. Установка зависимостей MCP серверов

```bash
cd mcp-servers
npm install
```

### 2. Тестирование серверов

```bash
# Тест Filesystem сервера
echo '{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}' | node filesystem-server.js /Users/ed/dev/kubik

# Тест Terminal сервера
echo '{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}' | node terminal-server.js

# Тест Web Reader сервера
echo '{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}' | node web-reader-server.js
```

## 🔧 Использование в Kilo Code

MCP серверы автоматически интегрируются с Kilo Code через конфигурацию в `.kilorc.json`. После настройки вы получаете доступ к дополнительным инструментам:

### Filesystem операции:
- Чтение кода из файлов
- Анализ структуры проекта
- Создание новых файлов
- Рефакторинг кода

### Terminal команды:
- Выполнение `npm install`, `npm run dev`
- Git операции
- Сборка и развертывание

### Web операции:
- Поиск документации
- Изучение лучших практик
- Анализ кода из репозиториев

### Browser автоматизация:
- Тестирование веб-приложений
- Скриншоты интерфейса
- Автоматизация рутинных задач

## 🛡️ Безопасность

### Уровни безопасности:

1. **Filesystem**: Только разрешенные директории
2. **Terminal**: Только whitelist команд
3. **Web Reader**: Только доверенные домены
4. **Browser**: Headless режим, ограниченные ресурсы

### Переменные окружения:

```bash
# Ограничение команд терминала
ALLOWED_COMMANDS="ls,cd,pwd,git,npm,node"

# Разрешенные домены для веб-доступа
ALLOWED_DOMAINS="github.com,stackoverflow.com,developer.mozilla.org"

# Токен GitHub для API доступа
GITHUB_TOKEN="your_github_token"
```

## 📊 Мониторинг и отладка

### Логи серверов:
```bash
# Каждый MCP сервер выводит логи в stderr
node filesystem-server.js /path/to/dir 2>&1 | tee server.log
```

### Проверка работоспособности:
```bash
# Проверка доступных инструментов
curl -X POST http://localhost:3001/api/test \
  -H "Content-Type: application/json" \
  -d '{"test": "mcp_servers"}'
```

## 🔄 Разработка новых MCP серверов

### Шаблон для нового сервера:

```javascript
import { Server } from '@modelcontextprotocol/sdk/server/index.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';

class CustomServer {
  constructor() {
    this.server = new Server({
      name: 'custom-server',
      version: '1.0.0',
    }, {
      capabilities: { tools: {} },
    });

    this.setupToolHandlers();
  }

  setupToolHandlers() {
    // Регистрация инструментов
    this.server.setRequestHandler(ListToolsRequestSchema, async () => {
      return {
        tools: [
          {
            name: 'custom_tool',
            description: 'Description',
            inputSchema: { /* schema */ },
          },
        ],
      };
    });

    // Обработка вызовов
    this.server.setRequestHandler(CallToolRequestSchema, async (request) => {
      // Логика инструмента
    });
  }

  async run() {
    const transport = new StdioServerTransport();
    await this.server.connect(transport);
    console.error('Custom MCP server running...');
  }
}

const server = new CustomServer();
server.run().catch(console.error);
```

## 🎯 Преимущества интеграции MCP

### Для разработчика:
- ✅ **Автоматизированные задачи**: Генерация кода, рефакторинг
- ✅ **Быстрый поиск**: Документация, примеры кода
- ✅ **Тестирование**: Автоматизированные тесты UI
- ✅ **Анализ**: Проверка кода, поиск уязвимостей

### Для проекта:
- ✅ **Повышение продуктивности**: Быстрая разработка
- ✅ **Качество кода**: Автоматический анализ и улучшения
- ✅ **Безопасность**: Контролируемый доступ к ресурсам
- ✅ **Масштабируемость**: Легко добавлять новые инструменты

## 📚 Документация

- [MCP Protocol Specification](https://modelcontextprotocol.io/specification)
- [Kilo Code Documentation](https://kilo-code.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Playwright Documentation](https://playwright.dev/docs)

## 🤝 Вклад в проект

Для добавления новых MCP серверов:

1. Создайте новый файл в `mcp-servers/`
2. Добавьте конфигурацию в `.kilorc.json`
3. Обновите `mcp-servers/README.md`
4. Создайте тесты в `__tests__/mcp/`

## 📞 Поддержка

При проблемах с MCP серверами:
1. Проверьте логи серверов
2. Убедитесь в корректности конфигурации
3. Проверьте переменные окружения
4. Проверьте сетевые подключения

---

**🎉 MCP серверы готовы к использованию! Теперь Kilo Code имеет расширенные возможности для веб-разработки.**